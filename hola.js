const names = process.argv.slice(2)

process.argv.forEach(name => 
    console.log(`Hola ${name}`)
    )


/* console.log(global)

console.log(process) */


//console.log(process.argv) //argument vector

//window (visual element) and document

//what we keep: -global and -process (globalThis) <--- is the same thing as "global"

//please check more on this. 

//all our commands originate from global or globalthis

